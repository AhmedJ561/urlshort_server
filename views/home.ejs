<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Server</title>
  </head>
  <body>
    <h1>All Shortened URLs</h1>
    <ul>
      <% urls.forEach(url => { %>
      <!-- ✅ Correct variable name and loop -->
      <li><%= url.shortId %></li>
      <!-- ✅ Correct syntax for output -->
      <% }); %>
    </ul>
  </body>
</html>
